
// Criar um array com os valores de 1 a 6
const numeros = [ 1, 2, 3, 4, 5, 6 ]

// Determinar o tamanho do array
console.log(numeros)
console.log(numeros.length)

// Adicionar os número de 7 a 10 (método push)
numeros.push(7, 8, 9, 10)

// Determinar o tamanho do array
console.log(numeros)
console.log(numeros.length)

// Remover os números 4 e 5 (método splice)
numeros.splice(3, 2)

// Determinar o tamanho do array
console.log(numeros)
console.log(numeros.length)
